# game-events

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build game-events` to build the library.

## Running unit tests

Run `nx test game-events` to execute the unit tests via [Jest](https://jestjs.io).
